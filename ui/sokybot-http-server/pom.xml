<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.sokybot</groupId>
        <artifactId>sokybot</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>sokybot-http-server</artifactId>
    <packaging>bundle</packaging>
    
    <name>sokybot-http-server</name>
    <description>Shared Reactor Netty HTTP Server and Router provider</description>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>sokybot-commons</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Reactor Netty - provided by dedicated Karaf feature -->
        <dependency>
            <groupId>io.projectreactor.netty</groupId>
            <artifactId>reactor-netty-http</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-core</artifactId>
            <scope>provided</scope>
        </dependency>
        
        <!-- Jackson - provided by sokybot-jackson feature -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <scope>provided</scope>
        </dependency>
        
        <!-- OSGi -->
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.framework</artifactId>
            <version>1.9.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.service.component.annotations</artifactId>
            <version>1.5.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.service.metatype.annotations</artifactId>
            <version>1.4.1</version>
            <scope>provided</scope>
        </dependency>
        
        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>${project.groupId}.${project.artifactId}</Bundle-SymbolicName>
                        <Bundle-Name>${project.name}</Bundle-Name>
                        <Bundle-Version>${project.version}</Bundle-Version>
                        <Bundle-Description>${project.description}</Bundle-Description>
                        <_dsannotations>*</_dsannotations>
                        <Export-Package>
                            org.sokybot.http.server,
                            org.sokybot.http.server.api,
                            org.sokybot.http.server.events
                        </Export-Package>
                        <_removeheaders>Require-Capability</_removeheaders>
                        <Import-Package>
                            <!-- Sun/Oracle internal packages (not available in OSGi) -->
                            !sun.*,
                            !com.sun.*,
                            !com.oracle.svm.*,
                            !org.graalvm.*,
                            !jdk.internal.*,
                            <!-- Optional compression libraries -->
                            !com.aayushatharva.brotli4j.*,
                            !com.github.luben.zstd.*,
                            !com.jcraft.jzlib.*,
                            !com.ning.compress.*,
                            !lzma.sdk.*,
                            !net.jpountz.*,
                            <!-- Optional protocols and security -->
                            !org.eclipse.jetty.*,
                            !com.google.protobuf.*,
                            !org.conscrypt.*,
                            !org.bouncycastle.*,
                            !org.jboss.marshalling.*,
                            !com.barchart.udt.*,
                            !gnu.io.*,
                            !io.netty.internal.tcnative.*,
                            <!-- Optional logging -->
                            !org.apache.log4j.*,
                            !org.apache.logging.log4j.*,
                            !org.apache.commons.logging.*,
                            <!-- Optional monitoring -->
                            !io.micrometer.*,
                            !io.opentracing.*,
                            <!-- Other optional -->
                            !org.apache.tomcat.*,
                            !com.fasterxml.aalto.*,
                            !org.codehaus.stax2.*,
                            !javax.annotation.meta,
                            !java.nio.file.spi,
                            <!-- Required -->
                            org.sokybot.commons.lifecycle;version="[1.0,2)",
                            com.fasterxml.jackson.*;version="[2.15,3)",
                            *
                        </Import-Package>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
