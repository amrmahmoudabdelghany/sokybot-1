# Maven Configuration
# This file contains default Maven options applied to all Maven commands

# Use parallel builds for faster compilation (use 4 threads)
-T4
-Daether.connector.basic.threads=1
-B
-ntp
