<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.sokybot</groupId>
        <artifactId>sokybot</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>sokybot-dist</artifactId>
    <packaging>pom</packaging>
    <name>Sokybot :: Distribution</name>
    <description>Custom Karaf distribution for Sokybot desktop application</description>

    <dependencies>
        <!-- Karaf framework base -->
        <dependency>
            <groupId>org.apache.karaf.features</groupId>
            <artifactId>framework</artifactId>
            <version>${karaf.version}</version>
            <type>kar</type>
        </dependency>
        
        <!-- Karaf standard features -->
        <dependency>
            <groupId>org.apache.karaf.features</groupId>
            <artifactId>standard</artifactId>
            <version>${karaf.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
        </dependency>
        
        <!-- Sokybot features -->
        <dependency>
            <groupId>org.sokybot</groupId>
            <artifactId>sokybot-features</artifactId>
            <version>${project.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Remove assembly data/tmp before archive (must run first in package phase) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>clean-assembly-tmp</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${project.build.directory}/assembly/data/tmp" failonerror="false"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Karaf assembly in prepare-package so we can clean data/tmp before archive -->
            <plugin>
                <groupId>org.apache.karaf.tooling</groupId>
                <artifactId>karaf-maven-plugin</artifactId>
                <version>${karaf.version}</version>
                <executions>
                    <execution>
                        <id>assembly</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>assembly</goal>
                        </goals>
                        <configuration>
                            <installedFeatures>
                                <feature>wrapper</feature>
                            </installedFeatures>
                            <startupFeatures>
                                <feature>eventadmin</feature>
                                <feature>log</feature>
                            </startupFeatures>
                            <bootFeatures>
                                <feature>standard</feature>
                                <feature>scr</feature>
                                <feature>http</feature>
                                <feature>sokybot-jackson</feature>
                                <feature>sokybot-full</feature>
                            </bootFeatures>
                            <archiveTarGz>false</archiveTarGz>
                            <archiveZip>true</archiveZip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>archive</id>
                        <phase>package</phase>
                        <goals>
                            <goal>archive</goal>
                        </goals>
                        <configuration>
                            <archiveTarGz>false</archiveTarGz>
                            <archiveZip>true</archiveZip>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <installedFeatures>
                        <feature>wrapper</feature>
                    </installedFeatures>
                    <startupFeatures>
                        <feature>eventadmin</feature>
                        <feature>log</feature>
                    </startupFeatures>
                    <bootFeatures>
                        <feature>standard</feature>
                        <feature>scr</feature>
                        <feature>http</feature>
                        <feature>sokybot-jackson</feature>
                        <feature>sokybot-full</feature>
                    </bootFeatures>
                    <archiveTarGz>false</archiveTarGz>
                    <archiveZip>true</archiveZip>
                </configuration>
            </plugin>

            <!-- Copy custom config files to the assembly (after assembly so etc is not overwritten) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-etc-configs</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/assembly/etc</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/resources/etc</directory>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- 
            Development profile: includes shell, SSH and dev tools 
            Usage: mvn clean install -P dev
        -->
        <profile>
            <id>dev</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.karaf.tooling</groupId>
                        <artifactId>karaf-maven-plugin</artifactId>
                        <version>${karaf.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <installedFeatures>
                                <!-- <feature>wrapper</feature> -->
                            </installedFeatures>
                            <bootFeatures>
                                <feature>bundle</feature>
                                <feature>config</feature>
                                <feature>scr</feature>
                                <feature>log</feature>
                                <feature>shell</feature>
                                <feature>ssh</feature>
                                <!-- Shared infrastructure features -->
                                <feature>sokybot-jackson</feature>
                                <!-- Application features -->
                                <feature>sokybot-dev</feature>
                            </bootFeatures>
                            <archiveTarGz>false</archiveTarGz>
                            <archiveZip>true</archiveZip>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- 
            Desktop distribution profile: creates bundled distribution for Electron
            Usage: mvn clean install -P desktop
        -->
        <profile>
            <id>desktop</id>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/resources/dist</directory>
                        <filtering>true</filtering>
                    </resource>
                </resources>
                <plugins>
                    <plugin>
                        <groupId>org.apache.karaf.tooling</groupId>
                        <artifactId>karaf-maven-plugin</artifactId>
                        <version>${karaf.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <!-- Minimal installation for desktop -->
                            <installedFeatures>
                                <feature>wrapper</feature>
                            </installedFeatures>
                            <bootFeatures>
                                <feature>standard</feature>
                                <feature>scr</feature>
                                <feature>http</feature>
                                <!-- Shared infrastructure features -->
                                <feature>sokybot-jackson</feature>
                                <!-- Application features -->
                                <feature>sokybot-full</feature>
                            </bootFeatures>
                            <!-- Disable server consoles for desktop -->
                            <startupFeatures>
                                <feature>eventadmin</feature>
                                <feature>log</feature>
                            </startupFeatures>
                            <archiveTarGz>false</archiveTarGz>
                            <archiveZip>false</archiveZip>
                        </configuration>
                    </plugin>
                    
                    <!-- Assembly plugin for creating desktop bundle -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>create-desktop-bundle</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/desktop-bundle.xml</descriptor>
                                    </descriptors>
                                    <finalName>${app.name}-${project.version}</finalName>
                                    <appendAssemblyId>true</appendAssemblyId>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
