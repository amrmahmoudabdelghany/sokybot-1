package org.sokybot.persistence.service;

import org.sokybot.persistence.entities.SectorRef;

/**
 * OSGi service interface for SectorRef persistence operations.
 */
public interface SectorRefRepository extends IRepository<SectorRef, Short> {
    // Additional specific methods can be added here if needed
}
